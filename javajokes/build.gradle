apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

sourceCompatibility = 1.7
